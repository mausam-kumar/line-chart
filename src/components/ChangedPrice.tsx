import { FC } from "react"

type ChangedPriceProps = {
    price: number
    percentageChange: number
}
const ChangedPrice: FC<ChangedPriceProps> = () => {
    return <p></p>
}

export default ChangedPrice